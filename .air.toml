# Config file for [Air](https://github.com/air-verse/air) in TOML format

# Working directory
# . or absolute path, please note that the directories following must be under root.
root = "."
tmp_dir = "bin"

[build]
cmd = "go build -o ./bin/yactd ./cmd/"
bin = "bin/main"
log = "air.log"
